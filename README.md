# My Portfolio (Vite + React + Tailwind)


This repository contains a simple portfolio built with React, Vite and Tailwind CSS. It's configured to deploy on Vercel.


## Run locally
```bash
npm install
npm run dev